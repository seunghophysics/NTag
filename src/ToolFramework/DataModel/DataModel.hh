#ifndef EVENTDATA_HH
#define EVENTDATA_HH

#include "EventVariables.hh"

class DataModel
{
    private:
        // Hits
        // Candidates
        // Event vars
        EventVariables eventVariables;
        // NTag vars (versioning, ntag options, etc)
};

#endif